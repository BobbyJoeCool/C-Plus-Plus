/*
/*
ASSIGNMENT 1 (Moderate) — Latin Square Validator (2D char array)

BACKGROUND:
A Latin square is an n-by-n array filled with n different letters, each occurring exactly once
in each row and exactly once in each column. The valid letters are the first n uppercase letters
starting from 'A' (A, B, C, ...).

GOAL:
Write a C++ program that:
1) Prompts the user for an integer n.
2) Reads an n x n grid of uppercase characters.
3) Validates that:
   - Every character is in the allowed set {'A'..('A'+n-1)}
   - Each row contains each allowed letter exactly once
   - Each column contains each allowed letter exactly once
4) Prints whether the grid IS a Latin square or is NOT.

INPUT EXAMPLE (n = 3):
Enter n: 3
Enter the 3x3 grid:
A B C
C A B
B C A

OUTPUT EXAMPLE:
Valid Latin square.

REQUIREMENTS:
- Use a 2D array OR a vector<vector<char>>.
- Use nested loops to validate rows and columns.
- Do not hardcode n.
- Clear, readable output.

NOTE:
You may treat spaces/newlines as separators (use cin >> ch).
*/

#include <iostream>
using namespace std;

int main()
{
    // TODO: Implement the program.
    return 0;
}